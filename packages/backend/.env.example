# Environment Configuration for MDCLodging
# Copy this file to .env and fill in your values

# =============================================================================
# REQUIRED VARIABLES (Application will not start without these)
# =============================================================================

# JWT Secret - MUST be 32+ characters and changed in production
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_change_in_production_min_32_characters_long

# JWT Token Expiration (optional, defaults to 7 days)
JWT_EXPIRES_IN=7d

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mdclodging
DB_USER=postgres
DB_PASSWORD=postgres
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/mdclodging

# =============================================================================
# PRODUCTION REQUIRED (Required when NODE_ENV=production)
# =============================================================================

# CORS Origin - Set to your production domain
# Example: https://yourdomain.com or https://app.mdclodging.com
CORS_ORIGIN=http://localhost:5173

# Node Environment (development, production, test)
NODE_ENV=development

# Server Port
PORT=3000

# =============================================================================
# RECOMMENDED (Application will run without these but functionality limited)
# =============================================================================

# SMTP Email Configuration (for password resets, notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=noreply@yourdomain.com
# SMTP_PASS=your_app_specific_password
# SMTP_FROM="MDCLodging <noreply@yourdomain.com>"
# SMTP_SECURE=false

# Error Tracking (Sentry)
# SENTRY_DSN=https://xxx@sentry.io/xxx

# Telegram Bot (for housekeeping notifications)
# TELEGRAM_BOT_TOKEN=your_bot_token_from_botfather
# TELEGRAM_BOT_ENABLED=false
# TELEGRAM_BOT_USERNAME=your_bot_username

# Application URL (for redirects, emails, webhooks)
# APP_URL=http://localhost:5173

# =============================================================================
# STRIPE PAYMENT INTEGRATION (SaaS Billing - Phase 2)
# =============================================================================
# Required for subscription billing and payments
# Get your keys from: https://dashboard.stripe.com/apikeys
# Webhook secret from: https://dashboard.stripe.com/webhooks

# STRIPE_SECRET_KEY=sk_test_51...
# STRIPE_PUBLISHABLE_KEY=pk_test_51...
# STRIPE_WEBHOOK_SECRET=whsec_...

# =============================================================================
# OPTIONAL (Performance & Storage)
# =============================================================================

# Redis (for rate limiting and caching - improves performance)
# REDIS_URL=redis://localhost:6379

# AWS S3 (for file uploads in production)
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# S3_BUCKET=mdclodging-uploads
# S3_BACKUP_BUCKET=mdclodging-backups

# =============================================================================
# FILE UPLOAD CONFIGURATION
# =============================================================================

# Upload directory
UPLOAD_DIR=./uploads

# Max file size (5MB default)
MAX_FILE_SIZE=5242880

# Allowed file types
ALLOWED_IMAGE_TYPES=image/jpeg,image/png,image/jpg
ALLOWED_EXCEL_TYPES=application/vnd.openxmlformats-officedocument.spreadsheetml.sheet,application/vnd.ms-excel

# =============================================================================
# RATE LIMITING
# =============================================================================

# Rate limit window in milliseconds (15 minutes default)
RATE_LIMIT_WINDOW_MS=900000

# Maximum requests per window
RATE_LIMIT_MAX_REQUESTS=100

# =============================================================================
# LOGGING
# =============================================================================

# Log Level (error, warn, info, debug)
LOG_LEVEL=info

# Log file path
LOG_FILE=logs/app.log

# Enable SQL query logging (true/false)
LOG_QUERIES=false
